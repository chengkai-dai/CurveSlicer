[project]
name = "curve-slicer"
version = "0.1.0"
description = "Differentiable curved slicing optimization for DLP 3D printing."
readme = "README.md"
requires-python = ">=3.9,<3.10"
dependencies = [
    "jax>=0.4.20",
    "jaxlib>=0.4.20",
    "numpy>=1.24",
    "openmesh>=1.2.1",
    "optax>=0.1.7",
]

[tool.uv]
package = false
